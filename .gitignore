# Raw HTML cache is ~100 MB and fully reproducible via scrape_fed.py
data/raw/
# Concatenated corpus is reproducible from data/clean/
data/fedspeak.txt
# Training artifacts
out/
__pycache__/
*.pyc
.DS_Store
# Logs
*.log
# Experiment sweep outputs (checkpoints); eval logs are copied to results/experiments/
runs/
data/ft/
finetune/out/
# Tokenised tensors are reproducible from data/clean via prepare.py
data/*.bin
data/meta.pkl
playground_gen.log
runs_*.log
finetune/out_*/

# Internal infrastructure notes: never commit to this public repo, never serve.
docs/PUBLISH_NEW_SITE.md

# Private interview prep notes
INTERVIEW_PREP.md
